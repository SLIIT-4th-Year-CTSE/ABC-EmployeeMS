apiVersion: v1
kind: Service
metadata:
  name: employee-service
  labels:
    name: employee-service
    app: demo-abc-app

spec:
  ports:
    - port: 5000
      targetPort: 5000
  selectors:
    name: employee-app-pod
    app: demo-abc-app
